---
- name: Dell OS10 
  hosts: all
  gather_facts: false
  collections: 
    - dellemc.os10
    - community.general.log_plays
  tasks:
    - name: Ping
      dellos10_command:
        commands:
          - show processes cpu summary
      register: result

    - name: Output
      debug: var=result.stdout_lines

